const modInfo = document.querySelector('#modInfo')
const addModButton = document.querySelector('#addModButton')

/*
async function addMod(e){
    e.preventDefault()
    console.log(addMod.value)
}
*/
/*

addModButton.addEventListener('click',e =>{
    e.preventDefault()
    console.log(modInfo.getAttribute('mod_id'))
    addMod(modInfo.getAttribute('mod_id'))
})

async function addMod(mod_id){
    response = await axios.post(`/mods/${mod_id}`)
}
*/