

/*
it('test functionName().  Insert description about the goal of this test., function() {
    //Write data for test.
        //let a = 10
        //let b = 20
    
    //Write tests.
        //expect(functionName(a,b)).toEqual(-10);
        //expect(functionName(b,a)).toEqual(10);
    
})

beforeEach() {
    //Function used to run code before each test is ran.
    //afterEach function exists as well.
    //There is also beforeAll and afterAll.
}
*/

/*

How do we add HTML data?

*/

describe("Test JS code for fetching mod information from Idgames.", () =>{
    xit("Receive info from Idgames website.",() => {
        //showMods()
    })
    xit("Convert info from Idgames into a proper Json object",() => {
        //jsonFormat()
    })
    xit("Create an HTML object out of the searched mod.",() => {
        //makeModObject()
    })
    xit("Hide all the buttons of the mod objects.",() => {
        //hideButtons()
    })
});

describe("Add mod info to Mods or Records tables.",()=>{
    xit("Send info to Mod table.",() => {
        //pullMod()
    })
    xit("Send info to Records table.",() => {
        //addMod()
    })
    xit("Turn pulled mod info in HTML into a JSON object.",() => {
        //jsonBuilder()
    })
});